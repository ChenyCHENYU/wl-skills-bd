package {{rootPackage}}.api.dto.{{module}};

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.Size;

/**
 * {{apiDesc}}({{Entity}})请求 DTO。
 * <p>用于新增/修改接口的请求体，必填字段带校验注解（@NotBlank/@NotNull/@Size）。
 *
 * @author wl-skills-bd codegen
 * @since 1.0.0
 */
@Getter
@Setter
@ToString
@Schema(description = "{{apiDesc}}DTO")
public class {{Entity}}DTO {

    {{#fields}}
    {{#required}}
    @NotBlank(message = "{{fieldComment}}不能为空")
    {{/required}}
    {{^required}}
    {{#notNull}}
    @NotNull(message = "{{fieldComment}}不能为空")
    {{/notNull}}
    {{/required}}
    {{#maxLength}}
    @Size(max = {{maxLength}}, message = "{{fieldComment}}长度不能大于{{maxLength}}")
    {{/maxLength}}
    @Schema(description = "{{fieldComment}}")
    private {{fieldType}} {{field}};

    {{/fields}}
}
