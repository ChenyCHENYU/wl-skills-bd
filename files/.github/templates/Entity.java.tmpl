package {{rootPackage}}.api.entity.{{module}};

import com.baomidou.mybatisplus.annotation.TableName;
import com.jhict.common.core.entity.CoreEntity;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Getter;
import lombok.Setter;
import lombok.experimental.Accessors;

/**
 * {{apiDesc}}({{Entity}})表实体类。
 * <p>映射数据库表 {{table}}，继承 CoreEntity 复用审计字段（id/companyId/isDelete/revision/create*/update*）。
 * <p>本类仅含业务字段，审计字段勿重复声明。
 *
 * @author wl-skills-bd codegen
 * @since 1.0.0
 */
@Getter
@Setter
@Accessors(chain = true)
@TableName("{{table}}")
@Schema(description = "{{apiDesc}}")
public class {{Entity}} extends CoreEntity {

    // ===== 业务字段（按表结构生成，勿手改结构）=====
    {{#fields}}
    @Schema(description = "{{fieldComment}}")
    private {{fieldType}} {{field}};

    {{/fields}}
}
